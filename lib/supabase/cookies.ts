/**
 * Год жизни у куки входа. По умолчанию @supabase/ssr ставит сессионную куку:
 * она умирает вместе с вкладкой, и установленное приложение при каждом запуске
 * просило войти заново. Токен всё равно проверяется и обновляется на сервере,
 * так что срок куки на безопасность не влияет.
 */
export const AUTH_COOKIE_OPTIONS = {
  maxAge: 60 * 60 * 24 * 365,
  sameSite: 'lax',
  path: '/',
} as const
